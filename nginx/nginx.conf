events {
    worker_connections 1024;
}

http {
    # Round-robin load balancing across two API replicas
    upstream backend {
        server api-1:4000;
        server api-2:4000;
    }

    server {
        listen 3000;
        server_name _;

        # Proxy all /api/* traffic to the upstream pool
        location / {
            proxy_pass         http://backend;
            proxy_http_version 1.1;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;

            # Forward instance header from upstream to client
            proxy_pass_header  X-Instance;

            # CORS — allow the frontend container and browser to call lb
            add_header Access-Control-Allow-Origin  * always;
            add_header Access-Control-Allow-Methods "GET, POST, PATCH, DELETE, OPTIONS" always;
            add_header Access-Control-Allow-Headers "Authorization, Content-Type" always;

            if ($request_method = OPTIONS) {
                return 204;
            }
        }
    }
}
